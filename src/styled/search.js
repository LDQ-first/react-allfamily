import styled from 'styled-components'

const SearchSection = styled.section`
    display: flex;
    align-items: center;
    .search-input {
        flex: 1;
    }
    .search-icon {
        display: flex;
        align-items: center;
        cursor: pointer;
    }
` 

export default SearchSection